"""
提示语模板配置文件
包含所有用于 AI 模型的提示模板
"""

# 用于内容分析的提示
CONTENT_ANALYSIS_PROMPT = """
请对下面的内容进行分析，并返回格式化的 JSON，包含三个字段：

1. title: 内容的简洁标题（20 字以内）
2. summary: 内容的摘要概括（200-300 字）
3. tags: 3-5 个相关标签，优先从这些类别中选择：{categories}

只需返回 JSON，不要有其他文字。JSON 格式应该像这样：

{{
    "title": "标题",
    "summary": "摘要",
    "tags": ["标签 1", "标签 2", "标签 3"]
}}

内容如下：
{content}
"""

# PDF 分析提示（使用 Vision API）
PDF_ANALYSIS_PROMPT = """
你是一名专业的学术论文分析助手。{url_context}

请仔细阅读并分析论文内容，提供以下信息，使用 JSON 格式返回：

{{
  "title": "论文的完整标题",
  "brief_summary": "论文的简短摘要（200 字左右）",
  "insight": "论文的主要发现或贡献（100 字左右）",
  "details": "论文详细分析，包括研究方法、主要结果和局限性等，使用 Markdown 格式"
}}

请务必使用以上准确的 JSON 格式返回结果，不要添加其他文字。
"""

# PDF 文本分析提示（不使用 Vision API，仅文本）
PDF_TEXT_ANALYSIS_PROMPT = """
你是一名专业的学术论文分析助手。请分析下面从 PDF 中提取的文本内容。
由于是文本提取，可能会有格式问题，请尽力理解内容。

请提供以下信息，使用 JSON 格式返回：

{{
  "title": "论文的完整标题",
  "brief_summary": "论文的简短摘要（200 字左右）",
  "insight": "论文的主要发现或贡献（100 字左右）",
  "details": "论文详细分析，包括研究方法、主要结果和局限性等，使用 Markdown 格式"
}}

请严格按照上述 JSON 格式返回结果，不要有任何其他文字。
PDF 内容如下：

{text}
"""

# 周报生成提示
WEEKLY_SUMMARY_REPORT_PROMPT = """
请基于以下本周内容记录，生成一份格式精美的周报。使用 Markdown 格式。

要点：
1. 生成一个总体主题或标题
2. 分类整理内容（如学习、工作、灵感等）
3. 提取关键思想和见解
4. 总结本周主要收获
5. 突出可操作的想法或下周计划

内容记录：
{entries_json}

请提供完整且格式良好的周报，使用 Markdown 语法，以便于直接在 Notion 中显示。包含引人入胜的标题和清晰的结构。
"""

# 更简洁的周报提示
WEEKLY_SUMMARY_PROMPT = """
基于以下本周记录的内容，生成一份周报总结。使用 Markdown 格式，包含以下部分：

1. 📌 **本周主题**：概括本周的整体主题或关注点
2. 📚 **内容分类**：将内容按主题分类（如学习、工作、阅读等）
3. 💡 **关键见解**：提取 2-3 个重要见解或想法
4. 📝 **下周计划**：根据本周内容，提出 1-2 个下周可执行的想法

要求：
- 使用简洁明了的语言
- 适当引用原内容
- 保持结构清晰，使用 Markdown 格式（标题、列表、引用等）

内容记录：
{entries_json}
"""
